package com.challenge.mercadolibre.core.service

object Endpoints {
    const val URL_BASE = "https://api.mercadolibre.com"
    const val GET_SEARCH = "/sites/{idSite}/search"
    const val GET_ITEM = "/items/{idItem}"
    const val GET_SITES = "/sites"
}